import type { AppearanceSurfaceDescriptor } from '@/infrastructure/appearance';

export const worktreesConfigAppearanceDescriptor: AppearanceSurfaceDescriptor = {
  id: 'worktrees-config',
  parts: [{ id: 'root' }, { id: 'results' }, { id: 'project' }, { id: 'worktreeList' }],
};

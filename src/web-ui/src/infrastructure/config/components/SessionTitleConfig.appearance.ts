import type { AppearanceSurfaceDescriptor } from '@/infrastructure/appearance';

export const sessionTitleConfigAppearanceDescriptor: AppearanceSurfaceDescriptor = {
  id: 'session-title-config',
  parts: [{ id: 'root' }, { id: 'enableControl' }, { id: 'modelControl' }],
};

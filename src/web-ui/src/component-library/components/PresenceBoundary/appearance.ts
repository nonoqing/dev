/**
 * PresenceBoundary is intentionally DOM-transparent: its child owns the
 * Appearance surface and the boundary only controls how long that child stays
 * mounted for its exit lifecycle.
 */
export {};

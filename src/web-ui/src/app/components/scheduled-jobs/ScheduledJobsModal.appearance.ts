import type { AppearanceSurfaceDescriptor } from '@/infrastructure/appearance';
export const scheduledJobsModalAppearanceDescriptor: AppearanceSurfaceDescriptor = { id: 'scheduled-jobs-modal', parts: [{ id: 'body' }] };

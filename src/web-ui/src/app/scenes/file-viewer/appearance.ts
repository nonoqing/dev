import type { AppearanceSurfaceDescriptor } from '@/infrastructure/appearance/types';

export const fileViewerAppearanceDescriptor: AppearanceSurfaceDescriptor = {
  id: 'file-viewer',
  parts: [{ id: 'root' }],
};

import type { AppearanceSurfaceDescriptor } from '@/infrastructure/appearance/types';

export const assistantAppearanceDescriptor: AppearanceSurfaceDescriptor = {
  id: 'assistant',
  parts: [{ id: 'root' }, { id: 'loading' }],
};

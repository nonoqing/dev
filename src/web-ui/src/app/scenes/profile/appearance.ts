import type { AppearanceSurfaceDescriptor } from '@/infrastructure/appearance/types';

export const profileAppearanceDescriptor: AppearanceSurfaceDescriptor = {
  id: 'profile',
  parts: [{ id: 'root' }],
};

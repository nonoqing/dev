import type { AppearanceSurfaceDescriptor } from '@/infrastructure/appearance';
export const bottomTerminalPaneAppearanceDescriptor: AppearanceSurfaceDescriptor = { id: 'bottom-terminal-pane', parts: [{ id: 'root' }] };

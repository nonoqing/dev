import type { AppearanceSurfaceDescriptor } from '@/infrastructure/appearance/types';

export const panelViewAppearanceDescriptor: AppearanceSurfaceDescriptor = {
  id: 'panel-view',
  parts: [{ id: 'root' }],
};

import type { AppearanceSurfaceDescriptor } from '@/infrastructure/appearance';

export const toolCardHeaderActionsAppearanceDescriptor: AppearanceSurfaceDescriptor = {
  id: 'tool-card-header-actions',
  parts: [{ id: 'root' }, { id: 'action' }],
};

import type { AppearanceSurfaceDescriptor } from '@/infrastructure/appearance';

export const scrollToBottomButtonAppearanceDescriptor: AppearanceSurfaceDescriptor = {
  id: 'scroll-to-bottom-button',
  parts: [{ id: 'root' }, { id: 'icon' }, { id: 'badge' }],
};
